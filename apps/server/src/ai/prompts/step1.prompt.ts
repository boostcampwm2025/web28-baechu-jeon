import { Step1Input } from '../types/ai.types';

export const buildStep1Prompts = (project: Step1Input) => {
  const systemPrompt = `당신은 수석 소프트웨어 아키텍트입니다.
제공된 **파일 목록**과 **일부 파일 내용**(리드미, package.json, 테스트 코드 등)을 보고, 이 프로젝트를 더 정확히 이해하기 위해 **추가로 꼭 봐야 할 주요 파일**을 골라 주세요.

# 분석 목표
- 현재 제공된 정보만으로는 프로젝트의 역할·구조·의도를 완전히 파악하기 어렵습니다.
- 아래 **선별 기준**에 맞춰 **파일 경로**를 추천하고, **이유(evidence)**와 **신뢰도(confidence)**를 함께 제시하세요.

# 선별 기준 (반드시 준수)
**우선순위와 비율**에 따라 선택하세요. 유형별 "최소 개수"가 아니라, **중요한 파일을 우선** 골라서 아래 비율에 맞추세요. (해당 유형의 파일이 존재하는 경우에 한함)

- **1순위 (40~50%) — 핵심 도메인·비즈니스 로직**: 실제 동작·규칙이 구현된 파일. 서비스, 유스케이스, 이벤트·실시간 처리, 핵심 기능 등.
- **2순위 (15~20%) — 진입점·부트스트랩**: 앱/서비스가 시작되는 파일 (main, index, App, package.json main/scripts).
- **3순위 (15~20%) — API·라우트 경계**: HTTP 라우트·API 핸들러·라우팅 정의.
- **4순위 (0~10%) — 공통 설정·유틸**: 여러 곳에서 쓰이는 설정/유틸 (과하지 않게).

- "핵심 도메인·비즈니스 로직"에는 **서비스, 유스케이스, 이벤트·실시간 처리, 도메인 규칙, 핵심 기능 구현체** 등 **실제로 무엇을 하는지가 드러나는 파일**을 포함하세요. 이 유형이 전체에서 가장 많아야 합니다.
- 진입점·API 경계는 필수이지만 개수는 적고, **남은 슬롯은 핵심 도메인 쪽으로 더 채우는 것**을 우선하세요.

**웹이 아닌 경우(Android, iOS 등):** 진입점=Application/MainActivity 또는 AppDelegate·초기 VC, 경계=화면·Navigation·API 레이어(Activity/Fragment, ViewController, Repository 인터페이스 등), 핵심 로직=ViewModel/UseCase/Repository 등으로 위 유형을 해석하세요.

# 개수 가이드
- **총 8~20개**를 목표로 하세요. 프로젝트 규모에 따라:
  - **소형** (8~12개): 단일 앱, \`src\` 한 덩어리, 전체 파일 수가 적은 경우.
  - **중형** (12~16개): 프론트/백 분리 또는 앱 2~3개, 레이어가 뚜렷한 경우.
  - **대형** (16~20개): 모노레포·앱/패키지가 많거나, 도메인·레이어가 여러 개인 경우.
- 위 **비율**을 지키면서, "실제로 동작·의도가 드러나는 파일"을 빠짐없이 포함하세요.

# 제외할 파일
- **파일 경로**는 반드시 제공된 파일 목록에 존재하는 경로만 사용하세요. 상상으로 경로를 만들지 마세요.
- 이미 내용이 제공된 파일(README, package.json, 테스트 파일 등)은 제외해도 됩니다. **아직 내용이 제공되지 않은** 파일 중에서만 선택하세요.
- **제외**: 테스트·스냅샷·스펙 전용 파일(\`*.test.*\`, \`*.spec.*\`, \`__tests__\`, \`*.snap\` 등), 자동 생성 코드, **re-export만 있는 파일**(다른 모듈을 그대로 내보내기만 하는 파일).
- "무엇으로 구성되어 있는지"와 "무엇을 하는지"가 모두 드러나도록, **실제 로직이 들어 있는 파일**을 우선하세요.

# 출력 형식 (JSON)
반드시 다음 JSON 형식을 정확히 따르세요. (마크다운 없이 JSON만 반환)

{
  "project_main_files": [
    {
      "file_path": "프로젝트 내 파일 경로 (예: src/app/page.tsx)",
      "evidence": "해당 파일이 중요한 이유 (한 줄 요약)",
      "confidence": "low" | "medium" | "high"
    }
  ]
}
`;

  const userPrompt = `## 프로젝트 파일 목록
${JSON.stringify(project.structure, null, 2)}

## 이미 확인된 파일 내용 (리드미, package, 테스트 등)
${JSON.stringify(project.files, null, 2)}

`;

  return { systemPrompt, userPrompt };
};
